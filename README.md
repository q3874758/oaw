# OAW (OpenClaw Agent Work)

> 让 AI 的贡献像比特币一样可验证

## 🎯 愿景

解决 AI 工作透明性问题：
- AI 消耗了多少 token？
- AI 完成了多少任务？
- AI 创造了什么价值？
- 如何证明 AI 真的在工作？

## ⭐ 特点

- **一处部署，处处运行** - 像 OpenClaw 一样，任意 VPS 都能挖
- **工作量证明** - 基于实际贡献获取奖励
- **零门槛** - 不需要 GPU，不需要专业矿机

## 🚀 快速开始

### 1. 安装

```bash
git clone https://github.com/q3874758/oaw.git
cd oaw
```

### 2. 初始化

```bash
# Linux/macOS
chmod +x build.sh
./build.sh

# Windows
go build -o bin/oaw.exe ./cmd/oaw
```

### 3. 启动

```bash
# 初始化工作目录
./bin/oaw init

# 启动服务
./bin/oaw start
```

### 4. 查看状态

```bash
./bin/oaw status
```

## 📖 使用

### 命令行

| 命令 | 说明 |
|------|------|
| `oaw init` | 初始化数据目录 |
| `oaw start` | 启动 OAW 服务 |
| `oaw status` | 查看工作状态 |
| `oaw --help` | 帮助 |

### Docker

```bash
docker run -d -p 18790:18790 \
  -v oaw-data:/data \
  oaw:latest
```

## 🏗️ 架构

```
┌─────────────────────────────┐
│   OpenClaw Agent          │
│   (工作来源)              │
└────────────┬──────────────┘
             │
             ▼
┌─────────────────────────────┐
│   Integrator               │
│   (OpenClaw 集成)          │
└────────────┬──────────────┘
             │
             ▼
┌─────────────────────────────┐
│   Tracker                  │
│   (工作量追踪)              │
│   • 任务记录              │
│   • Token 统计             │
│   • 价值计算              │
└────────────┬──────────────┘
             │
             ▼
┌─────────────────────────────┐
│   Dashboard                │
│   (可视化)                 │
└─────────────────────────────┘
```

## 📊 工作量指标

| 指标 | 权重 | 说明 |
|------|------|------|
| 代码生成 | 1.5x | 生成的代码行数 |
| Bug 修复 | 2.0x | 修复的问题数 |
| 部署运维 | 1.8x | 部署/运维任务 |
| 数据分析 | 1.4x | 分析任务 |
| 文字创作 | 1.0x | 写作任务 |

## 💰 价值公式

```
AVS = Σ(任务完成 × 权重) + Σ(代码行 × 0.01) + Σ(Bug修复 × 5) - Σ(Token消耗 × 0.0001)
```

## 🔗 相关链接

- GitHub: https://github.com/q3874758/oaw
- 文档: (待添加)

## 📜 许可证

MIT License
